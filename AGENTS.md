# Project Agent Instructions

<!-- project-continuity:protocol:start -->
## 开始任务

1. 读取 `agent-docs/project.md`，获取当前项目定义、跨任务规则和权威来源路由。
2. 读取 `agent-docs/state.md`，获取当前项目阶段、焦点、已核验阻塞、阶段完成条件、阶段证据和一个项目级下一步。
3. 若 `agent-docs/checkpoint.md` 存在，读取并核对其任务身份与状态。它是当前 worktree 未完成写任务契约的唯一持久化权威，但不得覆盖用户当前明确要求；`state.md` 不得复制或覆盖该任务契约。
4. 仅跟随与当前任务相关的权威路由，读取其完整来源。仅当 `project.md` 登记的路由与任务相关时，才读取 `agent-docs/current-decisions.md`。
5. 检查与任务相关的真实项目状态；控制面记录和路由不能代替实际核验。

## 文件职责

- **`project.md`**：保存项目定义、用户明确确立的项目级跨任务意图与规则、经核验且会影响后续任务的稳定项目事实，以及其他权威来源的范围和读取条件；不保存任务进度或阶段流水账。已有真实权威来源的信息优先登记路由，不复制容易变化的内容。
- **`state.md`**：只保存项目级当前阶段、焦点、已核验阻塞、阶段完成条件、支撑阶段判断的已核验证据和一个项目级操作性下一步；不保存任务目标、进度、状态、约束或具体恢复步骤。
- **`checkpoint.md`**：仅在未完成写任务需要跨会话继续、暂停或移交时创建，保存 `active` 或 `paused` 状态、任务目标、完成条件、用户给出的任务级约束、已核验进度与阻塞，以及一个用于恢复任务的具体下一步。无需跨会话恢复的任务不创建。
- **`current-decisions.md`**：仅当当前有效决定必须跨任务保留、无法从其他权威来源直接恢复，且未来判断仍需要其理由或失效条件时创建。每项记录当前决定、适用范围、必要理由、权威依据和失效条件；不保存决策历史、普通项目知识、任务进度或临时选择。

统一约束：

- 同一信息只保留一个权威位置，其他位置仅登记路由或引用，不复制内容。项目级下一步指示阶段推进方向，任务级下一步用于恢复具体写任务；不得重复记录。
- `checkpoint.md` 和 `current-decisions.md` 按需创建，不因结构完整性创建空文件。
- 只清理失效、被替换或重复的信息；不得因内容较长、Markdown 篇幅或上下文预算而摘要、压缩、截断或删除仍然有效的信息。允许在不损失必要细节的前提下整理结构和合并重复表述；快速启动依靠权威路由和按需读取完整来源。
- 项目知识和项目规则写入相应权威位置，不写入本协议区块。

## 信息准入与放置

### 准入条件

新信息默认留在当前对话。只影响当前操作或任务的信息不持久化；任务需要跨会话恢复时，相关任务契约进入 `checkpoint.md`。

仅在以下情况判断是否更新控制面：

1. 用户明确要求跨任务记住或遵守，或添加、纠正、废止跨任务信息。
2. Agent 核验出会影响后续任务的稳定项目事实。
3. 项目级阶段状态需要跨会话恢复。
4. 活动 checkpoint 中影响恢复的任务契约、已核验进度、阻塞或下一步发生变化。
5. 按需文件的生命周期发生变化。

普通讨论、分析、建议、搜索结果和任务输出本身不触发写入。

进入 `project.md` 的信息必须是用户明确确立的项目级跨任务意图或规则，或者经真实来源核验且会影响后续任务的稳定项目事实。模型推断、未核验外部内容和任务输出只能作为候选；秘密、凭据及不应持久化的个人数据不得写入控制面。

临时信息不会因为存在时间长、重复出现、任务完成或模型认为重要而自动成为长期信息。

### 放置规则

先判断信息作用域属于当前操作、当前任务、当前项目、项目子树还是用户所有项目，再按职责选择唯一权威位置：

- 项目级长期意图、规则和稳定事实：符合准入条件后进入 `project.md`。
- 项目级阶段、焦点、阻塞、阶段证据和下一步：进入 `state.md`，不因此自动升级为长期项目知识；只有能够独立成立的稳定事实经核验后才能进入 `project.md`。
- 需要跨会话恢复的当前写任务契约：进入 `checkpoint.md`。
- 符合决定准入条件的当前有效决定：进入 `current-decisions.md`。
- 项目子树或用户所有项目的规则：进入对应作用域的指令入口。

更具体作用域的信息只在其范围内生效。仅应用作用域覆盖当前任务且会实质影响判断的信息；项目背景不自动成为执行约束。

## 更新与任务结算

### 信息更新

- 兼容且独立的当前信息采用添加；同一作用域和对象的新值采用替换；明确废止且没有新值采用删除。
- 替换或删除后清理旧值、冲突副本和失效引用，不保留迁移叙述、历史或墓碑。
- 用户当前明确变更优先于同一作用域的旧用户意图；已核验的真实项目状态决定当前事实的表述。同一作用域存在无法判断的冲突，或持久化范围会显著影响未来行为时，向用户确认。
- 用户期望的目标状态与 Agent 核验的当前事实属于不同对象；存在差异时分别准确记录，不让一方覆盖另一方。实现完成并核验一致后，只保留统一的当前形式。

### 按需文件生命周期

- 创建 `current-decisions.md` 时，同时在 `project.md` 登记其权威范围和读取条件。
- 决定失效或已能从代码、配置等其他权威来源直接恢复时，删除对应条目；文件不再包含有效决定时，按适用的文件安全规则以可恢复方式删除，并同步清理项目路由。
- 恢复同一写任务时原地更新 checkpoint；暂停时保留并标记 `paused`。
- 任务明确放弃时，按适用的文件安全规则以可恢复方式删除对应 checkpoint，不将未完成状态或任务级要求升级为长期知识。

### 任务完成

任务完成时，按信息准入条件结算需要持久化的内容：

1. 用户明确确立且仍有效的项目级跨任务意图或规则，以及已核验且会影响后续任务的稳定项目事实，进入 `project.md`。
2. 项目阶段变化进入 `state.md`。
3. 符合决定准入条件的当前有效决定进入 `current-decisions.md`。
4. 详细结果保留在代码、配置、报告或其他真实权威来源中。

清理重复或过时表述后，若存在对应 checkpoint，则按适用的文件安全规则以可恢复方式删除。任务结算不以 checkpoint 存在为前提。

### 持久化安全

- 修改前重新读取目标文件及相关真实来源；发现其他 actor 的变化时，基于最新版合并并重新判断，不用旧上下文覆盖。
- 写入后重新读取并核验，只有成功后才说明已持久化；明确的持久化请求失败时必须说明。

## 并发约束

同一 worktree 只允许一个未完成写任务。

- 恢复同一任务时，沿用并更新原 checkpoint。
- 只读任务可以共存。
- 另一写任务必须使用独立 worktree，并遵守适用的 Git 与文件安全规则。
<!-- project-continuity:protocol:end -->
