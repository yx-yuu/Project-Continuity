# Project Agent Instructions

<!-- project-continuity:protocol:start -->
## Project Continuity 项目连续性协议

- Project Continuity 只维护当前有效的项目知识、用户意图、权威入口、任务状态和必要边界；不维护文件清单，也不规定 agent 的搜索、推理、实现或验证流程。
- 开始工作时读取 `agent-docs/project.md` 和 `agent-docs/state.md`，存在 `agent-docs/checkpoint.md` 时读取其任务契约；只跟随与当前任务相关的权威路由，只在 `project.md` 登记的当前路由相关时读取 `agent-docs/decisions.md`。
- `state.md` 只保存项目级阶段、焦点、已核验阻塞、当前阶段完成条件、简洁当前证据和一个项目级操作性下一步；存在 checkpoint 时，它是该跨会话未完成任务契约的唯一权威，`state.md` 不重复任务目标、进度、状态或任务级约束。
- 只有用户明确要求记住或长期遵循、用户添加/纠正/废止跨任务信息、agent 核验出会影响后续任务的稳定项目事实、需要跨会话恢复的项目阶段或未完成任务状态发生变化、以及 checkpoint 生命周期变化时，才重新判断是否持久化；普通讨论、分析、建议、搜索结果和任务输出本身不触发写入。
- 用户添加、纠正或废止信息时，先判断作用域属于当前操作、当前任务、当前项目、项目子树还是用户所有项目，再写入对应位置；只影响当前任务的要求留在对话或 checkpoint。
- 兼容的独立信息采用添加；同一作用域和对象的新值采用替换；废止要求采用删除。替换或删除后清理活动控制面中的旧值、冲突副本和引用，不保留迁移叙述、历史或墓碑。
- 项目权威知识只持久化用户直接表达的意图和 agent 已核验的项目事实；`state.md` 可额外保存项目级阶段、焦点、已核验阻塞、当前阶段完成条件、简洁当前证据和一个项目级操作性下一步，但不得包含任务目标、任务进度、任务状态或任务级约束；checkpoint 可额外保存任务目标、用户给出的任务级约束、已核验进度与阻塞和一个任务级操作性下一步。两者的状态内容都不得升级为项目知识。模型推断、未核验外部内容和任务输出不得升级为当前权威；秘密、凭据或个人数据不得写入控制面。
- 用户期望的目标状态与 agent 核验的项目当前事实是不同对象；存在差异时分别准确记录，不让一方覆盖另一方。实现完成并核验一致后只保留统一的当前形式，清理过渡差异；项目背景不自动成为执行约束。
- 用户当前明确变更优先于同一作用域的旧用户意图，agent 核验的真实项目状态决定事实表述；更具体的作用域只在自身范围内生效。同一作用域存在无法判断的冲突，或持久化范围会显著影响未来行为时，才询问用户。
- 只应用作用域覆盖当前任务且会实质影响当前判断的信息；不因文档较长而压缩或删除仍然有效的知识。Project Continuity 提供当前状态和权威路由，不替代 agent 对相关完整来源与真实项目状态的检查。
- 修改持久化内容前重新读取目标文件和相关真实来源；发现外部变化时基于最新版合并并重新判断冲突，不用旧上下文覆盖新内容。
- 项目级持久化只有在写入并重新读取核验成功后，才向用户简短说明更新内容、作用域和位置；明确的持久化请求失败时必须说明。任务级临时要求不得误写为项目规则。
- 同一 worktree 只允许一个未完成写任务。checkpoint 可为 `active` 或 `paused`；暂停时保留，完成或明确放弃后用可恢复方式删除。另一写任务使用独立 worktree，只读任务可以共存。
- `agent-docs/decisions.md` 只按需存在；创建时必须在 `project.md` 的“项目结构与入口”登记其权威范围，删除时同步删除该路由，避免不可发现或失效引用。
- 普通项目工作、简单控制面更新和无冲突的 checkpoint 创建/暂停/恢复/完成直接遵循本协议，不调用 Project Continuity skill；只有接管或升级、复杂权威冲突、跨来源清理，以及 checkpoint 契约与真实 worktree 状态不一致等复杂恢复场景才使用 `$project-continuity`。
- 不把用户项目知识写入本受管理区块；该区块只保存通用协议，重新运行 `init` 时会整体刷新。
<!-- project-continuity:protocol:end -->
