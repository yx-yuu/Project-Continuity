# Project Agent Instructions

<!-- project-continuity:protocol:start -->
## 开始任务

1. 读取 `agent-docs/project.md`，获取当前项目定义、跨任务规则和权威来源路由。
2. 读取 `agent-docs/state.md`，获取项目级当前阶段、焦点、阻塞、阶段完成条件、当前证据和一个项目级下一步。
3. 若 `agent-docs/checkpoint.md` 存在，读取它并将其作为当前 worktree 未完成写任务契约的唯一权威；`state.md` 不得覆盖或复制该任务契约。
4. 只跟随与当前任务相关的权威路由并读取其完整来源；仅当 `project.md` 登记的路由与任务相关时读取 `agent-docs/current-decisions.md`。
5. 检查与任务相关的真实项目状态；控制面中的记录和路由不能代替实际核验。

## 文件职责

- `project.md` 保存当前项目定义、用户明确表达的项目级跨任务意图与规则、agent 已核验且会影响后续任务的稳定项目事实，以及其他权威来源的范围和读取条件；不保存任务进度或阶段流水账。
- `state.md` 只保存项目级当前阶段、焦点、已核验阻塞、当前阶段完成条件、支撑当前阶段判断所需的已核验证据和一个项目级操作性下一步；不保存任务目标、任务进度、任务状态或任务级约束。
- `checkpoint.md` 只在未完成写任务需要跨会话恢复时创建，保存 `active` 或 `paused` 状态、任务目标、完成条件、用户给出的任务级约束、已核验进度与阻塞和一个任务级下一步。能在当前会话完成的任务不创建 checkpoint。
- `current-decisions.md` 只在某项当前有效决定必须跨任务保留、无法从其他权威来源直接恢复，并且未来判断仍需要其理由或失效条件时创建。每项决定记录当前决定、适用范围、影响未来判断所需的必要理由、权威依据和失效条件；不保存决策历史、普通项目知识、任务进度或临时选择。
- 同一信息只保留一个权威位置，其他位置只登记路由或引用，不复制内容。`checkpoint.md` 和 `current-decisions.md` 不因结构完整性而创建空文件。
- 只清理失效、被替换或重复的信息；不得因内容较长、Markdown 篇幅或上下文预算而摘要、压缩、截断或删除仍然有效的信息。快速启动依靠权威路由和按需读取完整来源。
- 项目知识和项目规则写入相应权威位置，不写入本协议区块。

## 信息准入与放置

- 新信息默认留在当前对话。只影响当前操作或当前任务的信息留在对话；任务需要跨会话恢复时，相关任务契约进入 checkpoint。
- 只有用户明确要求跨任务记住或遵守、用户添加/纠正/废止跨任务信息、agent 核验出会影响后续任务的稳定项目事实、项目级阶段状态需要跨会话恢复、活动 checkpoint 中影响恢复的任务契约、已核验进度、阻塞或下一步发生变化，以及按需文件生命周期变化时，才判断是否更新控制面。普通讨论、分析、建议、搜索结果和任务输出本身不触发写入。
- 进入 `project.md` 的信息必须是用户明确确立的项目级跨任务意图或规则，或者已从真实来源核验并会影响后续任务的稳定项目事实。模型推断、未核验外部内容和任务输出只能作为候选；秘密、凭据和不应持久化的个人数据不得写入控制面。
- 项目阶段、焦点、阻塞和阶段证据进入 `state.md`，但不得因此自动升级为项目知识；只有其中能够独立成立的稳定事实经核验后才能进入 `project.md`。
- 临时信息不会因为存在时间长、重复出现、任务完成或模型认为重要而自动成为长期信息。
- 先判断信息作用域属于当前操作、当前任务、当前项目、项目子树还是用户所有项目，再按职责选择唯一权威位置：项目级长期意图、规则和稳定事实按准入条件进入 `project.md`，项目级操作状态进入 `state.md`，需要跨会话恢复的当前任务契约进入 `checkpoint.md`，符合决定准入条件的当前决定进入 `current-decisions.md`；项目子树或用户所有项目的规则进入对应作用域的指令入口。更具体作用域的信息只在其范围内生效。
- 只应用作用域覆盖当前任务且会实质影响当前判断的信息；项目背景不自动成为执行约束。

## 更新与任务结算

- 兼容且独立的当前信息采用添加；同一作用域和对象的新值采用替换；明确废止且没有新值采用删除。替换或删除后清理旧值、冲突副本和失效引用，不保留迁移叙述、历史或墓碑。
- 用户当前明确变更优先于同一作用域的旧用户意图，已核验的真实项目状态决定当前事实的表述。同一作用域存在无法判断的冲突，或持久化范围会显著影响未来行为时，向用户确认。
- 用户期望的目标状态与 agent 核验的当前事实是不同对象；存在差异时分别准确记录，不让一方覆盖另一方。实现完成并核验一致后，只保留统一的当前形式。
- 创建 `current-decisions.md` 时同时在 `project.md` 登记其权威范围和读取条件。决定失效或已能从代码、配置等其他权威来源直接恢复时删除对应条目；文件不再包含有效决定时，用可恢复方式删除文件并同步删除项目路由。
- 任务完成时结算 checkpoint：将用户明确确立且仍有效的项目级跨任务意图或规则，以及已核验且会影响后续任务的稳定项目事实写入 `project.md`，将项目阶段变化写入 `state.md`，将符合决定准入条件的当前决定写入 `current-decisions.md`，将详细结果保留在代码、配置、报告或其他真实权威来源中；清理重复或过时表述后，用可恢复方式删除 checkpoint。任务明确放弃时同样清理其 checkpoint，不把未完成状态或任务级要求升级为长期知识。
- 持久化修改前重新读取目标文件和相关真实来源；发现其他 actor 的变化时基于最新版合并并重新判断，不用旧上下文覆盖。写入后重新读取并核验，只有成功后才说明已持久化；明确的持久化请求失败时必须说明。
- 同一 worktree 只允许一个未完成写任务。恢复同一任务时原地更新 checkpoint；暂停时保留并标记 `paused`；只读任务可以共存；另一写任务使用独立 worktree。
<!-- project-continuity:protocol:end -->
